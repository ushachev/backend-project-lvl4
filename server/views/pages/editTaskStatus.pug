extends ../layouts/application.pug

block content
  .row
    .col
      .h4 статус:
  .row
    .col
      form(action=`/taskStatuses/${values.id}`, method='post')
        +overrideInput('patch')
        .form-row
          .col-12.col-sm-8
            +input('name', null, null, values, errors)
          .col-12.col-sm-4
            +submit('изменить')(class='btn-block')
